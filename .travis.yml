sudo: false
language: node_js
node_js:
  - 10 # use nodejs v10 LTS
cache: npm
branches:
  only:
    - master # build master branch only
script:
  - hexo generate # generate static files
deploy:
  provider: pages
  skip-cleanup: true
  github-token: 7936e35e435d918dca826dd13e4987dafac256bd
  keep-history: true
  on:
    branch: master
  local-dir: public